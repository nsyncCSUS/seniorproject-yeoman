'use strict';

var should = require('should');
var app = require('../../../../app');
var request = require('supertest');


describe('POST /api/users/:id/groups/:groupId/events/:eventId', function() {
	it('should create a group', function(done) {
		done();
	});
});
