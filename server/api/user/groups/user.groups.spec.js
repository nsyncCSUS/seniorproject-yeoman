'use strict';

var should = require('should');
var app = require('../../../app');
var request = require('supertest');

describe('POST /api/users/:userId/groups', function() {
  it('should create a new group', function(done) {
    done();
  });
});


