'use strict';

var should = require('should');
var app = require('../../../../app');
var request = require('supertest');


describe('GET /api/users/:id/events/volunteeredTo', function() {
    it('should respond with a json array', function(done) {
        done();
    });
});


describe('GET /api/users/:id/events/volunteeredTo/:eventId', function() {
    it('should respond with a single event', function(done) {
        done();
    });
});


describe('POST /api/users/:id/events/volunteeredTo', function() {
    it('should add an event to a users volunteer list', function(done) {
        done();
    });
});


describe('DELETE /api/users/:id/events/volutneeredTo/:eventId', function() {
    it('should remove an event from a users volunteer list', function(done) {
        done();
    });
});
