'use strict';

var should = require('should');
var app = require('../../app');
var request = require('supertest');

describe('GET /api/groups/:id/subscriptions', function() {
    it('should respond with a json array', function(done) {
        done();
    });
});


describe('GET /api/groups/:id/subscriptions/:subscriptionId', function() {
    it('should respond with a single user', function(done) {
        done();
    });
});


describe('POST /api/groups/:id/subscriptions', function() {
    it('should add a user to a groups subscription list', function(done) {
        done();
    });
});


describe('DELETE /api/groups/:id/subscriptions/:subscriptionId', function() {
    it('should remove a user from a groups subscription list', function(done) {
        done();
    });
});